version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No preBuild steps required for static site"
    build:
      commands:
        - echo "No build steps required for static site"
  artifacts:
    baseDirectory: HTML-PAGES
    files:
      - '**/*'
      - '../css/*'
      - '../ASSETS/*'
      - '../JS/*'
  cache:
    paths: []
